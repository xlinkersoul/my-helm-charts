apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-insecure.fullname" . }}
  labels:
    {{- include "nginx-insecure.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-insecure.selectorLabels" . | nindent 4 }}
