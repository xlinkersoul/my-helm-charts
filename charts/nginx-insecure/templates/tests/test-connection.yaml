apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-insecure.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-insecure.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-insecure.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
